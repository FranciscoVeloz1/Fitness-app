<%-include ('components/_header.ejs') %>

<div class="contenedor__form">
    <div class="card" style="width: 24rem;">
        <div class="card-header">
            <p class="titulo">Template</p>
        </div>

        <div class="card-body">
            <form action="/update/<%= data.id %>" method="POST">
                <div class="form-group">
                    <input type="text" id="" name="" value="<%= data.name %>" placeholder="" class="form-control" autofocus>
                </div>
    
                <div class="contenedor__form">
                    <button type="submit" id="" name="" class="btn btn-primary"></button>
                </div>
            </form>
        </div>
    </div>
</div>

<%-include ('components/_footer.ejs') %>